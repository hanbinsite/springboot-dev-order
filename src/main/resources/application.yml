spring:
  datasource:
    url: jdbc:mysql://14.103.239.150:7118/dev-self?useSSL=false&serverTimezone=UTC
    username: dev
    password: Han147258
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data: 
    redis:
        host: 14.103.239.150
        port: 7102
        password: Han147258  # 无密码可省略
        database: 0
        lettuce:
            pool:
                max-active: 8     # 最大连接数
                max-idle: 8       # 最大空闲连接
                min-idle: 0       # 最小空闲连接
                max-wait: -1ms    # 获取连接最大等待时间